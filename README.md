# The LijOLo-Demo-Project

The project shows the use of liquibase, jooq & localstack.

## Requirements

To run the project, the installation of:

* Java 21
* Docker

is required.


## Vorbereitung

Im ersten Schritt folgendes Command ausführen:

```bash
docker volume create pg-admin-config
```
















